## Place to put your passwords. This vars will be used for the new servers we create in CLC

server_password: 'pleaseFillMeInWithSomethingSecure-this-will-be-your-root-password'
public_key: "ssh-rsa put the text of your public key here so this host can ssh without passwords to the new servers"
